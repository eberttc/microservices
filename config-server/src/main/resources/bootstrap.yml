spring:
  application:
    name: config-server-app
  cloud:
    config:
      server:
        native:
          search-locations: classpath:repository/{application}local/
  profiles:
    active: native
server:
  port: 8889

    
     