spring:
  application:
    name: client
  profiles:
    active: application,services
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
#      discovery:
#        enabled: true
